node_modules/
state/
*.log
.DS_Store
# Private material — never commit keys, tokens, journals, keystores, or run state
**/private/
**/*.key
**/*.pem
**/keystore*.json
**/funding-journal/
**/.env
**/run-state/

# Operator-local material. Extension patterns alone missed obvious names like
# keys/treasury.txt, so the whole directory is ignored.
keys/
config/secrets.json
.context/
*.secret.json
