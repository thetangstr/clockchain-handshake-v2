{
  "name": "@clockchain/handshake-sdk",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Offline Clockchain handshake SDK helpers; verification delegates only to caller-supplied trust, revocation, and replay callbacks.",
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@clockchain/handshake-protocol": "0.1.0"
  }
}
