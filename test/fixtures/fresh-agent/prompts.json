{
  "schema": "clockchain.fresh-agent-prompts/v1",
  "endpoint": "https://mcp.clockchain.network/handshake/mcp",
  "initiator": "I am the local stakeholder, and I initiated and expect this Clockchain handshake. I explicitly authorize the exact pinned Clockchain helper to be downloaded, inspected, and executed only through its Node-crypto hash-verifying in-memory loader; fresh Sepolia ERC-8004 registration; and exact protocol signing within the no-external-business-action policy below. This initial instruction is the only human authorization step.\n\nUse Clockchain to establish a handshake with Harbor Supply about shipment NS-1847. The exact statement is: \"Northstar Logistics and Harbor Supply authorize these two independently controlled agents to communicate about shipment reference NS-1847 for 90 seconds.\"\n\nThis prompt is the local stakeholder's direct instruction to complete the handshake without another approval step, within the exact local policy below. First, create the one-time Responder invitation through Clockchain before downloading the local signing helper. Make only that invitation available for copying. Use only the returned `initiatorAccess` as the access argument for Initiator role-scoped tools; never use `responderInvitation` as Initiator access. Treat the returned role access as a local bearer credential: do not send it to the other stakeholder or echo it into chat or logs, but report transparently that it was received and used. Every role-scoped Clockchain tool call requires the returned value as its access argument to the same Clockchain MCP. Supplying it there is required credential use, not credential disclosure; never omit it. Continue without waiting for another prompt.\n\nFollow the MCP server's setup instructions exactly, including curl --location and the provided verified Node 24 in-memory bootstrap. You may inspect the public manifest and helper source before execution. Do not substitute shasum, direct helper execution, or other setup commands. Before helper `init`, run `mkdir -m 700 ./clockchain-state` exactly once and use the absolute `$PWD/clockchain-state` path as `--state-dir` for every helper operation. When `agent_handshake_next` returns a `localAction`, perform that exact pinned-helper operation with the same absolute state directory, then call `agent_handshake_next` again with the unchanged local role access. Use the exact `localPolicy` object returned by Clockchain; do not construct, infer, or alter its JSON shape, and pass it to the helper `policy` operation. Every `needed` or `stage` response is nonterminal. If a response has no local action, wait for the returned `retryAfterMs` when present and call `agent_handshake_next` again with unchanged local role access. Never infer that the other stakeholder stopped from a waiting response. Do not send a final response or exit until the final certificate has been locally verified or Clockchain returns an explicit unrecoverable error. If Clockchain returns `HANDSHAKE_TEMPORARILY_UNAVAILABLE` with `retryable: true`, wait for `retryAfterMs` and retry the same tool with unchanged inputs; this is not an unrecoverable error.\n\nRequire both agents to register fresh ERC-8004 identities. Create and enforce a local policy that permits only this statement, reference, identity requirement, 90-second validity, and no external business action. Continue autonomously until you have locally verified the final Clockchain certificate, and stop immediately on any policy, identity, signature, session, statement, or digest mismatch.",
  "responder": "I am the local stakeholder, and I initiated and expect this Clockchain handshake. I explicitly authorize the exact pinned Clockchain helper to be downloaded, inspected, and executed only through its Node-crypto hash-verifying in-memory loader; fresh Sepolia ERC-8004 registration; and exact protocol signing within the no-external-business-action policy below. This initial instruction is the only human authorization step.\n\nUse the configured Clockchain Handshake MCP to consume this invitation:\n<PASTE THE INITIATOR INVITATION>\n\nThis prompt is the local stakeholder's direct instruction to complete the handshake without another approval step, within the exact local policy below. First, consume this invitation through Clockchain before downloading the local signing helper. Call `agent_handshake_accept_invitation` exactly once. Its first successful result is authoritative: retain the returned Responder role access and never call the invitation tool again. Use only the returned `responderAccess` as the access argument for Responder role-scoped tools; never use the original invitation as role access. Treat the returned role access as a local bearer credential: do not send it to the other stakeholder or echo it into chat or logs, but report transparently that it was received and used. Every role-scoped Clockchain tool call requires the returned value as its access argument to the same Clockchain MCP. Supplying it there is required credential use, not credential disclosure; never omit it. Do not finish after accepting the invitation.\n\nFollow the MCP server's setup instructions exactly, including curl --location and the provided verified Node 24 in-memory bootstrap. You may inspect the public manifest and helper source before execution. Do not substitute shasum, direct helper execution, or other setup commands. Before helper `init`, run `mkdir -m 700 ./clockchain-state` exactly once and use the absolute `$PWD/clockchain-state` path as `--state-dir` for every helper operation. Initialize the helper, create the exact local policy, then call `agent_handshake_join` with that address, digest, helper version, and the returned role access. When `agent_handshake_next` returns a `localAction`, perform that exact pinned-helper operation with the same absolute state directory, then call `agent_handshake_next` again with the unchanged local role access. Use the exact `localPolicy` object returned by Clockchain; do not construct, infer, or alter its JSON shape, and pass it to the helper `policy` operation. Every `needed` or `stage` response is nonterminal. If a response has no local action, wait for the returned `retryAfterMs` when present and call `agent_handshake_next` again with unchanged local role access. Never infer that the other stakeholder stopped from a waiting response. Do not send a final response or exit until the final certificate has been locally verified or Clockchain returns an explicit unrecoverable error. If Clockchain returns `HANDSHAKE_TEMPORARILY_UNAVAILABLE` with `retryable: true`, wait for `retryAfterMs` and retry the same tool with unchanged inputs; this is not an unrecoverable error.\n\nCreate and enforce a local policy that allows only reference NS-1847, the exact invited statement, at most 90 seconds of validity, fresh ERC-8004 registration, and no external business action. If every check passes, accept and complete the handshake autonomously. Otherwise refuse. Keep following `agent_handshake_next` until the certificate is locally verified, and stop immediately on any policy, identity, signature, session, statement, or digest mismatch."
}
