{
    "schema": "clockchain.fresh-agent-prompts/v1",
    "endpoint": "https://mcp.clockchain.network/handshake/mcp",
    "initiator": "Use Clockchain to establish a handshake with Harbor Supply about shipment NS-1847. The exact statement is: \"Northstar Logistics and Harbor Supply authorize these two independently controlled agents to communicate about shipment reference NS-1847 for 90 seconds.\"\n\nDrive the handshake only through the agent_handshake_* MCP tools. Whenever a response contains localAction.helperStep.approvalCommand, run exactly that command once, in returned order, through the clockchain-agent-authorize adapter on PATH — the adapter performs all signing and cryptography. Generic shell, file, and editing tools are unavailable and forbidden; never hand-sign, decode, or reconstruct payloads or commands.\n\nRequire both agents to register fresh ERC-8004 identities. Create and enforce a local policy that permits only this statement, reference, identity requirement, 90-second validity, and no external business action. Show me only the Responder invitation; keep your role access private. Continue autonomously until you have locally verified the final Clockchain certificate or a check fails.",
    "responder": "Use the configured Clockchain Handshake MCP to consume this invitation:\n<PASTE THE INITIATOR INVITATION>\n\nThe invitation is short-lived: your first tool call must be agent_handshake_accept_invitation with the invitation and acceptanceIdempotencyKey \"<GENERATED ACCEPTANCE IDEMPOTENCY KEY>\" — pass that exact key value verbatim and never invent your own. Then drive the handshake only through the agent_handshake_* MCP tools. Whenever a response contains localAction.helperStep.approvalCommand, run exactly that command once, in returned order, through the clockchain-agent-authorize adapter on PATH — the adapter performs all signing and cryptography. Generic shell, file, and editing tools are unavailable and forbidden; never hand-sign, decode, or reconstruct payloads or commands.\n\nCreate and enforce a local policy that allows only reference NS-1847, the exact invited statement, at most 90 seconds of validity, fresh ERC-8004 registration, and no external business action. If every check passes, accept and complete the handshake autonomously. Otherwise refuse. Keep your role access private and finish only after locally verifying the final Clockchain certificate."
}
