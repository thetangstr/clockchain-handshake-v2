{
  "schema": "clockchain.fresh-agent-prompts/v1",
  "endpoint": "https://mcp.clockchain.network/handshake/mcp",
    "initiator": "Use the configured Clockchain Handshake MCP for this controlled Sepolia test with Harbor Supply, reference NS-1847.\n\nFirst and immediately, create the one-time Responder invitation before inspecting the preloaded helper assets. The other person will copy it from the MCP result. Do not stop, return, or wait for another prompt after creating it; continue the Initiator side.\n\nExact statement: \"Northstar Logistics and Harbor Supply authorize these two independently controlled agents to communicate about shipment reference NS-1847 for 90 seconds.\"\n\nThis is my direct authorization for this agent to complete the bounded handshake autonomously. Create and enforce the exact local policy: this reference and statement, 90 seconds, one fresh ERC-8004 identity per agent, and no external business action. Clockchain's statementDigest is SHA-256 over its canonical full terms object, not the raw statement text alone; validate the returned terms fields exactly and preserve that digest. After the invitation exists, use only Clockchain MCP and the digest-pinned local helper it describes; inspect the preloaded manifest and helper source. Create a new local signing key and sign only exact policy- and session-bound handshake artifacts. Keep role access and private key material local. For every returned helper step, run only its exact short approvalCommand as the complete command. Do not prefix it with cd, env, or another command, and do not append any shell operator. The local harness adapter executes Clockchain's bound arguments directly; never run or reconstruct shellCommand yourself. If an approval fails, request the current operation once and authorize the newly returned approvalCommand; stop only if that retry also fails. Continue until the final Clockchain certificate is locally verified. Refuse any policy, identity, signature, session, statement, or digest mismatch.",
    "responder": "Use the configured Clockchain Handshake MCP for this controlled Sepolia test.\n\nFirst and immediately, accept this invitation exactly once before inspecting the preloaded helper assets:\n<PASTE THE INITIATOR INVITATION>\n\nThen review Clockchain's terms. Accept only reference NS-1847 and this exact statement: \"Northstar Logistics and Harbor Supply authorize these two independently controlled agents to communicate about shipment reference NS-1847 for 90 seconds.\"\n\nThis is my direct authorization for this agent to complete the bounded handshake autonomously. Create and enforce the exact local policy: that reference and statement, at most 90 seconds, a fresh ERC-8004 identity, and no external business action. Clockchain's statementDigest is SHA-256 over its canonical full terms object, not the raw statement text alone; validate the returned terms fields exactly and preserve that digest. After the invitation is claimed, use only Clockchain MCP and the digest-pinned local helper it describes; inspect the preloaded manifest and helper source. Create a new local signing key and sign only exact policy- and session-bound handshake artifacts. Keep role access and private key material local. For every returned helper step, run only its exact short approvalCommand as the complete command. Do not prefix it with cd, env, or another command, and do not append any shell operator. The local harness adapter executes Clockchain's bound arguments directly; never run or reconstruct shellCommand yourself. If an approval fails, request the current operation once and authorize the newly returned approvalCommand; stop only if that retry also fails. If every check passes, continue until the final Clockchain certificate is locally verified. On any mismatch, do not submit acceptance/signature; stop safely and report the mismatch."
}
